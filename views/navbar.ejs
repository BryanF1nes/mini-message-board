<nav class="border-b border-slate-200 bg-white px-8 shadow-sm">
  <ul class="mx-auto flex min-h-16 max-w-7xl items-center justify-between gap-2">

    <div class="flex items-center">
      <h4 class="mb-0 pr-6 text-xl font-bold tracking-tight text-slate-900">
        Mini Message Board
      </h4>

      <% for (let i = 0; i < links.length; i++) { %>
        <li class="relative">
          <a
            href="<%= links[i].href %>"
            class="group flex items-center rounded-[10px] px-4 py-3 text-[0.95rem] font-medium text-slate-700 transition-all duration-200 hover:bg-slate-100 hover:text-blue-600 active:translate-y-px"
          >
            <span><%= links[i].text %></span>

            <span
              class="absolute bottom-1 left-4 right-4 h-0.5 origin-center scale-x-0 bg-blue-600 transition-transform duration-200 group-hover:scale-x-100"
            ></span>
          </a>
        </li>
      <% } %>
    </div>

    <%- include('search') %>

  </ul>
</nav>
