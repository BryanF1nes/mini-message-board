<html>
    <head>
        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <%- include('navbar', { liks: links }); %>
        <div class="container">
            <div id="<%= message.id %>" class="card">
                <h4><%= message.username %></h4>
                <p><%= message.message %></p>
                <p><%= message.date_added.toLocaleString() %></p>
            </div>
        </div>
        <%- include('footer'); %>
    </body>
</html>
