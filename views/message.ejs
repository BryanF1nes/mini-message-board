<main class="mx-auto max-w-3xl px-6 py-10">
  <div
    id="<%= message.id %>"
    class="rounded-2xl border border-slate-200 bg-white p-6 shadow-md"
  >
    <h4 class="mb-3 text-xl font-semibold text-slate-900">
      <%= message.username %>
    </h4>
    <p class="mb-6 text-slate-700">
      <%= message.body %>
    </p>
    <div class="flex items-center justify-between text-sm text-slate-500">
        <p
          class="message-date text-sm text-slate-500"
          data-date="<%= message.date_added.toISOString() %>"
        >
          Loading...
        </p>
        <% if (currentUser && currentUser.id === message.user_id || currentUser && currentUser.role === 'admin') { %>
            <a href="/messages/<%= message.id %>/edit" class="font-medium text-blue-600 hover:text-blue-700">Edit</a>
        <% } %>
    </div>
  </div>
</main>
